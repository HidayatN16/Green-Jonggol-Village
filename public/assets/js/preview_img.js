function a1(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a1').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a2(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a2').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a3(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a3').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a4(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a4').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a5(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a5').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a6(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a6').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a7(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a7').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a8(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a8').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a9(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a9').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a10(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a10').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a11(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a11').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a12(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a12').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}

function a13(input) {
    if (input.files && input.files[0]) {
        var reader = new FileReader();

        reader.onload = function(e) {
            document.getElementById('a13').setAttribute('src', e.target.result);
        }

        reader.readAsDataURL(input.files[0]);
    }
}